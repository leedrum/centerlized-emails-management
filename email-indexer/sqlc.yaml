version: "2"
sql:
  - schema: "db/schema.sql"
    queries: "db/queries"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_interface: false
        emit_json_tags: true
        emit_prepared_queries: false
        emit_exact_table_names: true
